# Required for /Decide — get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional — defaults to claude-haiku-4-5
# ANTHROPIC_MODEL=claude-haiku-4-5

# Harper Fabric deploy targets (not needed for local dev)
CLI_TARGET_USERNAME='YOUR_CLUSTER_USERNAME'
CLI_TARGET_PASSWORD='YOUR_CLUSTER_PASSWORD'
CLI_TARGET='YOUR_FABRIC.HARPER.FAST_CLUSTER_URL_HERE'
